apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Login-service.fullname" . }}-test-connection"
  labels:
    {{- include "Login-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Login-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
