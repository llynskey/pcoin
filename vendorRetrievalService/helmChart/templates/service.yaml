apiVersion: v1
kind: Service
metadata:
  name: {{ include "vendor-retrieval-service.fullname" . }}
  labels:
    {{- include "vendor-retrieval-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vendor-retrieval-service.selectorLabels" . | nindent 4 }}
