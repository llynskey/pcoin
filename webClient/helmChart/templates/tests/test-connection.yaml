apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-client.fullname" . }}-test-connection"
  labels:
    {{- include "web-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
